
public interface TimeCalculations {
	
	//checks if the animal is an adult
	public boolean isAdult();
	
	//checks if the animal is dead
	public boolean isDead();
	
	//returns the number of items produced in one month
	public int increaseProduct();

}
